# Copy to secrets.yaml (auto-added to .gitignore)
prod:
  # SSH: password OR ssh_key_path (key takes priority if both present)
  password: YOUR_SSH_PASSWORD
  # ssh_key_path: ~/.ssh/id_rsa

  # DB: separate password (omit to reuse `password`)
  # db_password: YOUR_DB_PASSWORD
